#include <iostream>

int main()
{
	/*
	 * If  both aren't equal, calculate the difference between the two, move the longer one forward to basically even both 
	 * Lists out and then iterate through both and search where two elements point at the same position.
	 */
}

